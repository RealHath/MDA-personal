name: Sync Upstream Release

on:
  schedule:
    - cron: '7 12 * * *'  # UTC+8 20:07 检查一次
  workflow_dispatch:

jobs:
  check-and-sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Check upstream release
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          UPSTREAM_TAG=$(gh release view --repo 1204244136/MDA --json tagName --jq .tagName 2>/dev/null || echo "")
          if [ -z "$UPSTREAM_TAG" ] || [ "$UPSTREAM_TAG" = "null" ]; then
            echo "No upstream release found"
            echo "should_run=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "Upstream latest tag: $UPSTREAM_TAG"

          if git tag | grep -q "^${UPSTREAM_TAG}$"; then
            # tag 存在时检查 release 是否已创建（上次可能触发 build 失败）
            if gh release view "$UPSTREAM_TAG" --repo "${{ github.repository }}" &>/dev/null; then
              echo "Already synced at ${UPSTREAM_TAG} with release"
              echo "should_run=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            echo "Tag exists but no release found, will retry build"
          fi

          echo "New upstream release: ${UPSTREAM_TAG}"
          echo "should_run=true" >> "$GITHUB_OUTPUT"
          echo "tag=$UPSTREAM_TAG" >> "$GITHUB_OUTPUT"

      - name: Sync upstream code
        if: steps.check.outputs.should_run == 'true'
        run: |
          git remote add upstream https://github.com/1204244136/MDA.git
          git fetch upstream --no-tags
          git checkout main

          # 配置 git 身份，用于 merge/commit
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git config merge.ours.driver true

          # 合并上游代码，保留 fork 特有的跳过验证版本
          if git merge upstream/main --no-edit; then
            echo "Merge succeeded"
          else
            echo "Merge conflicts detected, auto-resolving fork-specific files"
            for f in \
              agent/go-service/taskersink/membership/action.go \
              agent/go-service/taskersink/membership/memberdata.go \
              agent/go-service/taskersink/membership/register.go; do
              if [ -f "$f" ]; then
                git checkout --ours -- "$f"
                git add "$f"
              fi
            done
            git commit --no-edit
          fi

          # 合并完成后，强制保留 memberdata.go/action.go/register.go 的 fork 版本
          for f in \
            agent/go-service/taskersink/membership/action.go \
            agent/go-service/taskersink/membership/memberdata.go \
            agent/go-service/taskersink/membership/register.go; do
            git checkout HEAD~1 -- "$f"
            git add "$f"
          done

          # 选择性覆盖 interface.json 中 fork 特有的字段
          node -e "
            const fs = require('fs');
            const path = 'assets/interface.json';
            const cfg = JSON.parse(fs.readFileSync(path, 'utf8'));
            cfg.github = 'https://github.com/RealHath/MDA-personal';
            fs.writeFileSync(path, JSON.stringify(cfg, null, 4) + '\n');
          "
          git add assets/interface.json

          # 删除上游带过来的 format.yml（GITHUB_TOKEN 无法推送 workflow 文件）
          rm -f .github/workflows/format.yml
          git add .github/workflows/format.yml 2>/dev/null || true

          git commit --no-edit -m "chore: preserve fork-specific overrides" || true

          git push origin main

          # 创建 tag 推送，触发 install.yml 自动打包发布
          TAG=${{ steps.check.outputs.tag }}
          if git tag | grep -q "^${TAG}$"; then
            # tag 已存在，强制更新到当前 HEAD
            git tag -f "$TAG"
          else
            git tag "$TAG"
          fi
          git push -f origin "$TAG"

      - name: Trigger build
        if: steps.check.outputs.should_run == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG=${{ steps.check.outputs.tag }}
          echo "Triggering install.yml for $TAG..."
          gh workflow run install.yml --repo "${{ github.repository }}" --ref main -f version="$TAG"
          echo "✅ Build triggered for $TAG, check it at:"
          echo "   https://github.com/${{ github.repository }}/actions/workflows/install.yml"
